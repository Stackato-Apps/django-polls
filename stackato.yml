name: django-polls
services:
  ${name}-db: postgresql
requirements:
  ubuntu:
    - libpq-dev
hooks:
  post-staging:
    - python manage.py syncdb --noinput
    - python manage.py collectstatic --noinput
